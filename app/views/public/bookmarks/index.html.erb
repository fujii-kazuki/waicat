<h1>Public::Bookmarks#index</h1>
<p>Find me in app/views/public/bookmarks/index.html.erb</p>

<h2>ブックマーク一覧</h2>

<% if current_user.bookmarks.any? %>
  <ul>
    <% current_user.bookmarks.each do |bookmark| %>
      <li>
        <%= link_to bookmark.cat.publication_title, cat_path(bookmark.cat.id) %>
        <div id=<%="bookmark-#{bookmark.cat.id}"%>>
          <%= render 'button', cat_id: bookmark.cat.id %>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>ブックマークされた掲載はありません。</p>
<% end %>