<li class="<%= 'current-user-message' if current_user.id == message.user.id %>">
  <p>
    <%= current_user.id == message.user.id && message.readed_flag ? '既読' : '　' %>
  </p>
  <p>
    <%= safe_join(message.body.split("\n"),tag(:br)) %>
  </p>
</li>